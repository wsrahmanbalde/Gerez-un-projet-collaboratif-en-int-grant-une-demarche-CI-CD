name: CI + Analyse SonarCloud

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-backend:
    name: Tests Java + JaCoCo
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: back
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - run: mvn clean verify
      - run: mvn jacoco:report
      - name: Vérifier jacoco.exec
        run: |
          ls -lh target/
          test -f target/jacoco.exec && echo "jacoco.exec trouvé !" || (echo "jacoco.exec manquant !" && exit 1)
      - uses: actions/upload-artifact@v4
        with:
          name: jacoco-exec
          path: back/target/jacoco.exec

  test-frontend:
    name: Tests Angular
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: front
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - run: npm ci
      - run: npm run test -- --watch=false --browsers=ChromeHeadless --code-coverage
      - name: Vérifier lcov.info
        run: |
          find coverage -name lcov.info
          test -f coverage/bobapp/lcov.info && echo "lcov.info trouvé !" || (echo "lcov.info manquant !" && exit 1)
      - uses: actions/upload-artifact@v4
        with:
          name: lcov-frontend
          path: front/coverage/bobapp/lcov.info

      # 👉 AJOUTE CETTE ÉTAPE ICI
      - name: Recompiler le backend (sans tests) pour SonarCloud
        run: mvn clean compile -DskipTests
        working-directory: back

      - name: Vérifier les fichiers téléchargés
        run: |
          ls -lh back/target/
          ls -lh back/target/classes/
          ls -lh front/coverage/bobapp/

      - name: Installer SonarCloud Scanner
        run: npm install -g sonar-scanner


  analyse-sonarcloud:
    name: Analyse complète SonarCloud (Back + Front)
    runs-on: ubuntu-latest
    needs: [test-backend, test-frontend]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Télécharger jacoco.exec
        uses: actions/download-artifact@v4
        with:
          name: jacoco-exec
          path: back/target/jacoco.exec

      - name: Télécharger lcov.info
        uses: actions/download-artifact@v4
        with:
          name: lcov-frontend
          path: front/coverage/bobapp

      - name: Vérifier les artifacts téléchargés
        run: |
          ls -lh back/target/
          ls -lh front/coverage/bobapp/

      - name: Installer SonarCloud Scanner
        run: npm install -g sonar-scanner

      - name: Analyse SonarCloud (Java + Angular)
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          sonar-scanner \
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }} \
            -Dsonar.organization=${{ secrets.SONAR_ORG }} \
            -Dsonar.sources=back/src,front/src \
            -Dsonar.java.binaries=back/target/classes \
            -Dsonar.coverage.jacoco.reportPaths=back/target/jacoco.exec \
            -Dsonar.javascript.lcov.reportPaths=front/coverage/bobapp/lcov.info \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.login=${{ secrets.SONAR_TOKEN }}
