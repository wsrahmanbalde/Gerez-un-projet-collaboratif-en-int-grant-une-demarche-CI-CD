name: CI - Pipeline général

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  echo:
    name: 🔧 Pipeline de base
    runs-on: ubuntu-latest
    steps:
      - name: Pipeline actif
        run: echo "Déclencheur CI/CD ok"